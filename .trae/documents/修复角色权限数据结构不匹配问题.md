我将执行以下操作来修复“分配权限”功能的问题：

1.  **修改权限服务 (Backend Service)**:
    *   修改 `backend/internal/service/permission.go` 文件。
    *   在 `AddPolicy`、`RemovePolicy` 和 `GetPolicies` 方法中，不再使用 `roleID` 拼接字符串（如 `role_1`）作为 Casbin 的 Subject。
    *   改为先通过 `roleID` 查询数据库获取 `Role` 对象，然后使用 `Role.Ident` 字段作为 Subject（例如直接使用 `admin`）。

2.  **优化权限处理器 (Backend Handler)**:
    *   修改 `backend/api/handler/permission_handler.go` 文件。
    *   在 `GetPolicies` 方法中，将 Casbin 返回的原始二维数组 `[][]string` 转换为结构化的 JSON 对象列表。
    *   返回格式将调整为：`[{"sub": "admin", "obj": "/api/users", "act": "GET"}, ...]`。
    *   这将解决前端因数据格式不匹配而无法正确显示已分配权限的问题。

此计划同时解决了您提出的“使用ident绑定”的需求以及之前的“权限获取错误”的问题。

请确认执行。